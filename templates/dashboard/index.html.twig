{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Dashboard{% endblock %}

{% block body %}
    <h1 class="text-center mt-3 mb-3 mx-auto">Dashboard</h1>
    <div class="container">
        {% for theme, content in themes %}
            <a class="card {{ theme }}" href="{{ path(content[2]) }}">
                <div class="imgBx">
                    <i class="fas fa-{{ content[0] }} fa-7x mt-2"></i>
                </div>
                <div class="contentBx">
                    <h2>Pôle {{ theme }}</h2>
                    <div class="info">
                        <p>{{ content[1] }}</p>
                    </div>
                </div>
            </a>
        {% endfor %}
    </div>

{% endblock %}
